# Force LF line endings for shell scripts (critical for Docker on Windows)
*.sh text eol=lf

# Ensure consistent line endings for config files mounted into containers
*.yml text eol=lf
*.yaml text eol=lf
